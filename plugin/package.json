{
  "name": "plugin",
  "version": "1.0.0",
  "description": "Agora meeting example plugin",
  "main": "script/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "install-macOS": "node-gyp clean && node-gyp rebuild --target=12.0.0 --dist-url=https://npm.taobao.org/mirrors/atom-shell && install_name_tool -add_rpath '@loader_path' ./build/Release/agora_plugin.node",
    "install-win": "node-gyp clean && node-gyp rebuild --arch=ia32 --target=12.0.0 --dist-url=https://npm.taobao.org/mirrors/atom-shell"
  },
  "author": "sylar",
  "license": "ISC",
  "gypfile": true,
  "dependencies": {
    "download": "^8.0.0",
    "extract-zip": "^2.0.1",
    "fs-extra": "^10.1.0",
    "glob": "^8.0.3",
    "just-task": "^1.5.0",
    "node-gyp": "8.3.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.6",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  }
}
