import { makeStyles, createStyles } from '@mui/styles';

const useStyle = makeStyles(() => {
  return createStyles({
    dialog: {
      // width: 640,
      // height: 480,
    },
  });
});

export default useStyle;
